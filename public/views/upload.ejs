<!DOCTYPE html>
<html>

<head>
    <title>f10w3r_wargame</title>
    <meta charset="utf-8">
    <style id="theme-color">
        body {
            background-color: black;
            color: white;
            margin: 0;
            font-family: Arial, sans-serif;
        }

        h1 {
            text-align: center;
            color: white;
            margin-top: 40px;
            font-size: 28px;
        }

        nav {
            display: flex;
            justify-content: space-between;
            padding: 8px 40px;
            background-color: #222;
            position: relative;
        }

        .dropdown-content {
            display: none;
            flex-direction: column;
            background-color: #222;
            opacity: 0.8;
            width: 100%;
            text-align: center;
            position: absolute;
            left: 50%;
            transform: translate(-50%, 10px);
            z-index: 1;
        }

        .dropdown-content a {
            color: white;
            padding: 10px 14px;
            text-decoration: none;
            display: block;
            width: 100%;
        }

        .dropdown-content a:hover {
            background-color: #3a3a3a;
        }

        .dropdown.active .dropdown-content {
            display: flex;
        }

        .dropdown button {
            padding: 8px 30px;
            background-color: #222;
            color: white;
            border: none;
            cursor: pointer;
        }

        .brand {
            color: white;
            text-decoration: none;
            font-size: 20px;
        }

        .form-container {
            display: flex;
            justify-content: center;
            align-items: center;
            margin-top: 60px;
            width: 100%;
        }

        .form-background {
            background: #333;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.5);
            width: 400px;
            max-width: 90%;
        }

        .form-background h1 {
            margin-top: 0;
            font-size: 26px;
        }

        .form-container input[type="text"],
        .form-container textarea,
        .form-container select {
            margin: 12px 0;
            padding: 12px;
            font-size: 13px;
            width: 100%;
            border-radius: 5px;
            border: 1px solid #ccc;
            background-color: #444;
            color: white;
            box-sizing: border-box;
        }

        .form-container textarea {
            resize: vertical;
        }

        .form-container button {
            margin: 12px 0;
            padding: 12px;
            background-color: #444;
            color: white;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
            box-sizing: border-box;
        }

        .form-container button:hover {
            background-color: #555;
        }

        .form-container a {
            color: #bbb;
            text-decoration: none;
            font-size: 14px;
        }

        .form-container a:hover {
            color: white;
        }

        /* 입력 칸 크기 조정 */
        .form-container .small-input {
            height: 40px;
        }

        .form-container .large-input {
            height: 180px;
        }

        /* 파일 입력 커스텀 스타일 */
        .form-container .file-input-container {
            position: relative;
            margin: 12px 0;
        }

        .form-container .file-input {
            opacity: 0;
            position: absolute;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            cursor: pointer;
        }

        .form-container .file-label {
            display: block;
            padding: 12px;
            background-color: #444;
            color: #bbb;
            font-size: 16px;
            text-align: center;
            border-radius: 5px;
            cursor: pointer;
            border: 1px solid #ccc;
        }

        .form-container .file-label:hover {
            background-color: #555;
            color: white;
        }

    </style>
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            const dropdownButton = document.querySelector('.dropdown button');
            const dropdownMenu = document.querySelector('.dropdown');

            dropdownButton.addEventListener('click', function() {
                dropdownMenu.classList.toggle('active');
            });

            // 파일 선택 시 파일 이름 표시
            const fileInput = document.querySelector('.file-input');
            const fileLabel = document.querySelector('.file-label');

            fileInput.addEventListener('change', function() {
                if (this.files && this.files.length > 0) {
                    fileLabel.textContent = this.files[0].name;
                } else {
                    fileLabel.textContent = 'Choose a file';
                }
            });
        });
    </script>
</head>

<body>
    <nav>
        <div>
            <a href="/" class="brand">f10w3r_wargame</a>
        </div>
        <div class="dropdown">
            <button type="button">
                Menu ▿
            </button>
            <div class="dropdown-content">
                <a href="/user">Users</a>
                <a href="/wargame">Wargame</a>
                <a href="/wargame/upload">Upload</a><br>
                <a href="/login">Login</a>
                <a href="/logout">Logout</a>
                <a href="/signup">Signup</a>
            </div>
        </div>
    </nav>

    <div class="form-container">
        <div class="form-background">
            <h1>Upload Page</h1>
            <form method="post" >
                <input type="text" name="title" placeholder="Title" required class="small-input"><br>
                <textarea name="text" placeholder="Text" required class="large-input"></textarea><br>
        
                <select name="category" required class="small-input">
                    <option value="">Select Category</option>
                    <option value="Web">Web</option>
                    <option value="Pwn">Pwn</option>
                    <option value="Reversing">Reversing</option>
                    <option value="Forensic">Forensic</option>
                    <option value="Crypto">Crypto</option>
                    <option value="Misc">Misc</option>
                    <option value="Web3">Web3</option>
                    <option value="etc">etc</option>
                </select><br>
                <input type="text" name="flag" placeholder="Flag" required class="small-input"><br>
                <div class="file-input-container">
                    <label for="file" class="file-label">Choose a file</label>
                    <input type="file" name="file" id="file" class="file-input" accept=".zip">
                </div>
                <button type="submit">Submit</button>
            </form>
        </div>
    </div>
</body>

</html>
